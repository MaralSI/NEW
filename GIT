git init - инициализация
gссылку на репозиторий


git add . - добавление
git commit -m 'название' - точка сохранения
git push origin master\ main - отправка в гитХАБ

MaraSI
файл .gitignore
это файл, который скрывает
1) ненужные файлы (мусор) или
2) важные коды (боты Телеграмм)

полезные команды
git status - на каком статусе
git rm --cached: удаляет файлы
git rm --cached -r: удаляет папку

git clone url - ссылка
git remote remove origin

git branch - показывает ветки *
git branch - создает ветку
git checkout name - переключает ветку
git merge name - обновляет ветку
git checkout -b name - создает и сразу на нее переключается





